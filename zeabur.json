{
  "build": {
    "env": {
      "DEBIAN_FRONTEND": "noninteractive"
    },
    "commands": [
      "sudo apt-get update",
      "sudo apt-get install -y ffmpeg",
      "pip install -r requirements.txt"
    ]
  },
  "run": {
    "cmd": "python main.py"
  }
} 